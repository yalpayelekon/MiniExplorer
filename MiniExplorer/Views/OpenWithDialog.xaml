<Window x:Class="MiniExplorer.Views.OpenWithDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:loc="clr-namespace:MiniExplorer.Localization"
        Height="360"
        Width="480"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource Brush.Background}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ListBox x:Name="AppsList"
                 DisplayMemberPath="Name"
                 MouseDoubleClick="Open_Click"/>
        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,12,0,0">
            <Button Width="90" Margin="0,0,8,0" Click="Open_Click"
                    Content="{Binding Source={x:Static loc:LocalizationNotifier.Instance}, Path=Revision, Converter={StaticResource Loc}, ConverterParameter=Dialog_OpenWithOpen}"/>
            <Button Width="130" Margin="0,0,8,0" Click="ChooseOther_Click"
                    Content="{Binding Source={x:Static loc:LocalizationNotifier.Instance}, Path=Revision, Converter={StaticResource Loc}, ConverterParameter=Dialog_OpenWithOther}"/>
            <Button Width="90" IsCancel="True" Click="Cancel_Click"
                    Content="{Binding Source={x:Static loc:LocalizationNotifier.Instance}, Path=Revision, Converter={StaticResource Loc}, ConverterParameter=Common_Cancel}"/>
        </StackPanel>
    </Grid>
</Window>
